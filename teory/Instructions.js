//Инструкции
// Инструкция заканчивается точкой с запятой, не требуется ; после блока инструкций
//получается есть выражения, инструкции и  выражение - инструкция только 3 сущности

let a
a = a + 3 // выражение
let b
b = b +10; // инструкция так как есть;



const number = 10 // expression
let d = 10; // instruction

const myFn = (value) => {
    return value  * 10;
}

console.log(myFn(number))
console.log(myFn(d));
console.log(myFn(3 + 5));